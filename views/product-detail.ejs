<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Chi tiết sản phẩm - <%= product.name %></title>

  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    a.back-link {
      font-weight: 600;
      color: #0d6efd;
      text-decoration: none;
      transition: color 0.3s ease;
    }
    a.back-link:hover {
      color: #084298;
      text-decoration: underline;
    }
  </style>
</head>
<body class="bg-light">

  <main class="container py-5">
    <div class="row g-4 align-items-center">
      
      <div class="col-md-5 text-center">
        <% if (product.image) { %>
          <img src="/uploads/<%= product.image %>" alt="<%= product.name %>" class="img-fluid rounded shadow" style="max-height: 350px; object-fit: contain;" />
        <% } else { %>
          <div class="bg-secondary text-white d-flex align-items-center justify-content-center rounded" style="height: 350px;">
            <span class="fs-4">No Image</span>
          </div>
        <% } %>
      </div>

      <div class="col-md-7">
        <h1 class="display-5 fw-bold text-primary mb-3"><%= product.name %></h1>
        <p class="fs-4 fw-semibold text-success mb-3">Giá: <%= product.price %> VNĐ</p>
        
        <p><strong>Mô tả:</strong></p>
        <p class="mb-4 text-gray-700"><%= product.description %></p>
        
        <p><strong>Tồn kho:</strong> <span class="badge bg-info text-dark fs-6"><%= product.stock %></span></p>

        <a href="/" class="back-link mt-4 d-inline-block">&larr; Quay lại danh sách</a>
      </div>

    </div>
  </main>

  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
